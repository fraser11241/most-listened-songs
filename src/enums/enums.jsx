export const SpotifyItemTypes = Object.freeze({
	ARTIST: 0,
	TOP_TRACK: 1,
	RECENT_TRACK: 2,
});

export const TimeRanges = Object.freeze({
    LONG_TERM: 2,
    MEDIUM_TERM: 1,
	SHORT_TERM: 0,
});

export const MessageState = Object.freeze({
	ERROR: 0,
	SUCCESS: 1
});